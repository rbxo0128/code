name: Manual Blog Post

on:
  workflow_dispatch:
    inputs:
      platform:
        description: '문제 플랫폼'
        type: choice
        options:
          - BOJ
          - Programmers
        required: true

      difficulty:
        description: '난이도 선택 (BOJ: Bronze/Silver/Gold/... Programmers: 0~5)'
        type: string
        required: true

      problem_id:
        description: '문제 번호 (쉼표로 여러 개 입력 가능, 예: 1027,1002,1003)'
        type: string
        required: true

      post_immediately:
        description: '즉시 포스팅 여부'
        type: boolean
        default: true

jobs:
  post-to-blog:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Post to Blog (multiple)
        if: github.event.inputs.post_immediately == 'true'
        env:
          BLOG_API_URL: ${{ secrets.BLOG_API_URL }}
          BLOG_API_KEY: ${{ secrets.BLOG_API_KEY }}
        run: |
          PLATFORM="${{ github.event.inputs.platform }}"
          DIFFICULTY="${{ github.event.inputs.difficulty }}"
          IDS="${{ github.event.inputs.problem_id }}"

          if [[ "$PLATFORM" == "BOJ" ]]; then
            BASE_DIR="백준"
          else
            BASE_DIR="프로그래머스"
          fi

          # 쉼표로 분리
          IFS=',' read -ra PROBLEM_IDS <<< "$IDS"

          for ID in "${PROBLEM_IDS[@]}"; do
            ID=$(echo "$ID" | xargs)  # 공백 제거
            echo "🔎 탐색: $BASE_DIR/$DIFFICULTY/$ID.*"
            FOUND_PATH=$(find "$BASE_DIR/$DIFFICULTY" -maxdepth 1 -type d -name "${ID}.*" | head -n 1)

            if [[ -z "$FOUND_PATH" ]]; then
              echo "❌ 문제 폴더를 찾을 수 없습니다: $BASE_DIR/$DIFFICULTY/$ID.*"
              continue
            fi

            echo "📁 포스팅 시작: $FOUND_PATH"
            PROBLEM_PATH="$FOUND_PATH" python .github/scripts/post_single_problem.py

            echo "⏱ 5초 대기..."
            sleep 5
          done

      - name: Show Preview Only
        if: github.event.inputs.post_immediately != 'true'
        run: |
          echo "📋 미리보기 모드 — 실제 포스팅되지 않았습니다."
